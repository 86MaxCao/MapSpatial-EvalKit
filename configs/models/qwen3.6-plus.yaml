# qwen3.6-plus via API (DashScope OpenAI-compatible endpoint)
name: qwen3.6-plus
backend: api

generate:
  temperature: 0.0
  max_new_tokens: 2048

backend_args:
  api_url: ${DASHSCOPE_API_URL}
  api_model: qwen3.6-plus

strategies: [direct]
