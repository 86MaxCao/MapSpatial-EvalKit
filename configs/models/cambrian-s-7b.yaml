# Cambrian-S-7B-LFP via cambrian backend
name: Cambrian-S-7B-LFP
backend: cambrian
model_path: ${CKPT_DIR}/Cambrian-S-7B-LFP

load:
  device: cuda:0
  dtype: bfloat16

generate:
  temperature: 0.0
  max_new_tokens: 2048

backend_args:
  conv_template: qwen_2

strategies: [direct]
