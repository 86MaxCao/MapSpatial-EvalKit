# ViLaSR via vilasr backend (multi-step spatial reasoning)
name: ViLaSR
backend: vilasr
model_path: ${CKPT_DIR}/ViLaSR

load:
  dtype: bfloat16

generate:
  temperature: 0.0
  max_new_tokens: 2048

backend_args:
  max_steps: 10
  timeout: 30

strategies: [direct]
